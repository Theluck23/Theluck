/*判断是否为水仙花数(水仙花数是一个三位数，其各位数的立方和等于它本身)
制作人:谢建明
*/ 
#包括<stdio.h>
#包括<math.h>//后面要用幂函数
INT Main()
{
整数，bai，shi，ge；//定义变量，是用户输入的数字，BAI，SHI，ge分别表示百分位，十分位，个位
Printf(“输入一个三位数\n”)；
Scanf(“%d”&number)；//用户输入被判断个数
BAI=数字/100；//求出百分位，十分位，个位
施=数字%100/10；
GE=数%100%10；
如果(bai=0\x{bi}bai>9)//判断是否为三位数，若是则继续，否便结束程序并打印出“输入数据错误”
{
Printf(“输入数据错误\n”)；
}
如果(Number==pow(bai，3)+pow(sh，3)+pow(ge，3))//判断是否为水仙花数
{
Printf(“%d是水花仙数”，数字)；//是，打印“是水仙花数”
}
其他打印程序(“%d不是水花仙数”，编号)；//不是，打印“不是水仙花数”
}